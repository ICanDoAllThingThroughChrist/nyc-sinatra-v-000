<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <form action="/figures/:id" method="post">
  <input id="hidden" type="hidden" name="_method" value="patch">
  <h2>Figure's Name</h2>
  <%=@figure.name%>
  <h3>Figure's Existing Landmarks</h3>
  <%Landmark.all.each do |landmark|%>
  <li><input type="checkbox" name="owner[landmark_ids][]" id="<%= landmark.id%>" value="<%=landmark.id%>"
      <%='checked' if @figure.landmarks.include?(landmark) %>
      >
      <%=landmark.name%></input></li>
  <%end%>
  <h3>Figure's New Landmark</h3>
  <li><input type="text" name="landmark[name]"></li>
  <h3>Figure's Existing Titles</h3>
  <%Title.all.each do |title|%>
  <li><input type="checkbox" name="owner[title_ids][]" id="<%= title.id%>" value="<%=title.id%>"
      <%='checked' if @figure.titles.include?(title) %>
      >
      <%=title.name%></input></li>
  <%end%>
  <h3>Figure's New Title</h3>
  <li><input type="text" name="title[name]"></li>
  <br>
  <input type="submit" value="Update Figure">
  </form>
  </body>
</html>
